/**
 * DEPRECATED: Use axios instead
 * 
 * This file is deprecated. Use the axios instances from utils/axios.js instead:
 * - axiosPublic: For public endpoints (no auth required)
 * - axiosPrivate: For private endpoints (auth required)
 * 
 * Example:
 * import { axiosPublic, axiosPrivate } from '../utils/axios'
 * 
 * const data = await axiosPublic.get('/products')
 * const user = await axiosPrivate.get('/users/me')
 */

export default {}
